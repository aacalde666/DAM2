package Ejercicio10;

public class Enunciado {

	
/*	Ejercicio10:

		Crea la clase Casa. Una casa tiene calle, numero, codArea (un número de 1 a 10 que representa en qué área está), superficie, plantas.
		Una Casa tiene los métodos:
		    • dirección() que escribe “esta casa está situada en <calle><numero>, codArea:< codArea >”.
		    • numHabitaciones() que indica cuantas habitaciones tiene (una habitación ocupa 10 m2 de una planta).
		    • estaEnArea(int codArea) que devuelve true si el codArea pasado como parámetro es el mismo del codArea de la casa.
		
		Crea la clase Pueblo que contiene un array de Casa (Este array no puede contener celdas null)
		Pueblo tiene los métodos:
		    • construyeCasa(Casa c): añade la casa al array
		    • destruyeCasa(Casa c): quita la casa del array
		    • superficieConstruida(): devuelve la suma de las superficie de todas las casas
		
		Crea la clase Region que contiene un array de Pueblo (este array tiene inicialmente 3 posiciones instanciadas con null. 
		Cada vez que el array se llena y se quiere poner otro pueblo, el array duplica su tamaño)
		Region tiene los métodos:
		    • fundaNuevoPueblo() que añade un nuevo Pueblo
		    • cuentaCasas() que devuelve cuantas casas hay en la región
		    • cuentaCasas(codArea) que devuelve cuantas casas hay en un codArea concreto
		    • habitacionesEnLaRegion() que cuenta cuantas habitaciónes hay en la región.
		
		Crea una Clase Estado con un main que use las clases y los métodos creados.
*/
 
}
